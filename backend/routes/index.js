import express from 'express';

const router = express.Router();

/* Verify JWT Token */
// router.route('/verify-token').get(authCtrl.verifyJWT);

/**
 * This is the code for the logout route.
 */
// router.route('/logout').get(authCtrl.logout);

export default router;